@using TransGGP.Application.Security
@model List<TransGGP.Domain.Models.Operador>
@{
    var puedeEditar = User.TienePermiso(Permisos.OperadoresEditar);
}

<div class="ggp-page-head">
    <div>
        <div class="ggp-eyebrow">Flota</div>
        <h1>Operadores</h1>
    </div>
    <div class="d-flex gap-2 flex-wrap">
        @if (puedeEditar)
        {
        <a asp-action="Create" class="btn btn-primary">+ Nuevo operador</a>
        }
    </div>
</div>

@if (!Model.Any())
{
    <div class="ggp-vacio"><p>Aún no hay operadores registrados.</p></div>
}
else
{
    <div class="table-responsive">
    <table class="table table-striped">
        <thead>
            <tr>
                <th>Id</th>
                <th>Número de operador</th>
                <th>Nombre</th>
                @if (puedeEditar)
                {
                    <th>Acciones</th>
                }
            </tr>
        </thead>
        <tbody>
            @foreach (var op in Model)
            {
                <tr>
                    <td>@op.Id</td>
                    <td>@op.NumeroOperador</td>
                    <td>@op.Nombre</td>
                    @if (puedeEditar)
                    {
                        <td>
                            <a asp-action="Edit" asp-route-id="@op.Id" class="btn btn-primary btn-sm">Editar</a>
                            <form asp-action="Delete" method="post" onsubmit="return confirm('¿Eliminar este operador?');" style="display:inline;">
                                <input type="hidden" name="id" value="@op.Id" />
                                <button type="submit" class="btn btn-danger btn-sm">Eliminar</button>
                            </form>
                        </td>
                    }
                </tr>
            }
        </tbody>
    </table>
    </div>
}
